{
  "name": "nuxtAppTest",
  "version": "1.0.0",
  "description": "Testing nuxt project setup",
  "author": "bjorn",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run build && npm run generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {}
}
